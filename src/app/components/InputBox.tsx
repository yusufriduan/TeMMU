interface inputBoxProp {
  icon: string;
  placeholderText: string;
  width: number;
  height: number;
}

export default function InputBox({
  icon,
  placeholderText,
  width,
  height,
}: inputBoxProp) {}
